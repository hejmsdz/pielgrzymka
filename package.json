{
    "scripts": {
        "start": "charge serve src",
        "build:html": "charge build src dist",
        "build:sw": "workbox generateSW workbox-config.js",
        "build": "npm run build:html"
    },
    "devDependencies": {
        "@notionhq/client": "^1.0.4",
        "@static/charge": "^1.7.0",
        "dotenv": "^16.0.1",
        "google-spreadsheet": "^3.3.0",
        "workbox-cli": "^6.5.3"
    },
    "dependencies": {
        "@picocss/pico": "^1.5.3"
    }
}
