{
    "scripts": {
        "start": "charge serve src/pages",
        "build:html": "charge build src/pages dist",
        "build:sw": "workbox generateSW workbox-config.js",
        "build": "npm run build:html && npm run build:sw"
    },
    "devDependencies": {
        "@static/charge": "^1.7.0",
        "workbox-cli": "^6.5.3"
    },
    "dependencies": {
        "@picocss/pico": "^1.5.3"
    }
}
